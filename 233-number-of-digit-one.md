# 233-数字 1 的个数[困难]

---

> <https://leetcode.cn/problems/number-of-digit-one>

[[17.06-number-of-2s-in-range-lcci.md]]

#剑指Offer #数字相关

---

这类问题在算法中统称为 **“数位 DP”**（数位动态规划）或 **“按位统计/逐位分析”**。

无论是统计数字 1、数字 2 还是其他非零数字，解题逻辑是**完全通用**的。

## 解题核心思路：按位统计

为了防止超时（因为 $n$ 最高可达 $10^9$，如果逐个遍历数字累加 $1$ 的个数，时间复杂度是 $O(n \log n)$，必然超时），我们需要**逐位分析**：分别计算个位、十位、百位……上出现 $1$ 的次数，最后将它们累加。

假设我们当前正在分析某一位 $d$（比如十位，即 `digit = 10`）：

我们把 $n$ 拆成三部分：

1. **高位部分** `high = n / (digit * 10)`

2. **当前位数值** `cur = (n / digit) % 10`

3. **低位部分** `low = n % digit`

根据当前位数字 `cur` 的取值，当前位出现 $1$ 的次数分为 **三种情况**：

1. `cur == 0`

   例如：$n = 2304$，分析十位（`digit = 10`），此时 `high = 23`, `cur = 0`, `low = 4`。
   - 只有当高位为 $0$ 到 $22$ 时，十位才可能出现 $1$（即 $001x \sim 221x$）。

   - 算式：`high * digit` $\rightarrow 23 \times 10 = 230$ 次。

2. `cur == 1`

   例如：$n = 2314$，分析十位（`digit = 10`），此时 `high = 23`, `cur = 1`, `low = 4`。
   - 高位取 $0 \sim 22$ 时，十位出现 $1$ 的次数为：`high * digit`（$230$ 次）。

   - 高位取 $23$ 时，十位依然是 $1$，此时受低位约束，低位可以取 $0 \sim 4$（即 $2310 \sim 2314$），共有 `low + 1` 次。

   - 算式：`high * digit + low + 1` $\rightarrow 230 + 4 + 1 = 235$ 次。

3. `cur > 1`

   例如：$n = 2324$，分析十位（`digit = 10`），此时 `high = 23`, `cur = 2`, `low = 4`。
   - 高位可以取 $0 \sim 23$（即 $001x \sim 231x$），十位都能完整出现 $10$ 次 $1$。

   - 算式：`(high + 1) * digit` $\rightarrow (23 + 1) \times 10 = 240$ 次。

> 由于该位是2，因此可以完整地获取该位是1时的10个结果，也就是$23*10+1*10$。

**注意**：我们是每次只统计某一位上的1的数量（如个位），统计范围是所有范围内的数字。之后再在这个范围内统计其余位上2的数量（十位/百位...）。

### Java 代码实现

```Java
class Solution {
    public int countDigitOne(int n) {
        // digit 表示当前分析的位数：1 代表个位，10 代表十位，100 代表百位 ...
        long digit = 1;
        int count = 0;

        while (n >= digit) {
            long high = n / (digit * 10);
            long cur = (n / digit) % 10;
            long low = n % digit;

            if (cur == 0) {
                count += high * digit;
            } else if (cur == 1) {
                count += high * digit + low + 1;
            } else { // cur > 1
                count += (high + 1) * digit;
            }

            digit *= 10; // 移动到下一位
        }

        return count;
    }
}
```

### 复杂度分析

- **时间复杂度**：$O(\log_{10} n)$。由于 $n$ 最多有 10 位数（$10^9$），`while` 循环最多执行 10 次，属于常数级别的时间，效率极高。

- **空间复杂度**：$O(1)$，仅使用了几个变量。

### 答疑：如果是统计数字 2 呢？

如果是统计数字 2（比如程序员面试金典中的“2的个数”）：

- 分界点只须调整为 `cur < 2`、`cur == 2` 和 `cur > 2`。

- 逻辑完全一致，唯一需要额外注意的地方是：如果是统计数字 0，最高位不能算作 0（即需要排除前导零的影响）。

## 书中思路：递归 + 排列组合

《剑指 Offer》书中的这个解法，核心思想是 **“递归 + 排列组合”**。

书中以 $n = 21345$ 为例，我们通过解答你的两个关键疑问，来彻底厘清它的逻辑。

### 一、 为什么要切分数字？

直接算 $1 \sim 21345$ 中 1 的个数非常困难，因为 **$21345$ 不是一个整十、整百、整千的数**，末尾有头有尾（如 $345$），无法直接套用简单的数学公式。

切分数字的根本目的：**把“不规则的范围”拆成“可以用数学公式直接算的规则整块” + “规模更小的同类子问题”。**

### 二、 为什么要那样切分？（切成 $1 \sim 1345$ 和 $1346 \sim 21345$）

作者把 $21345$ **直接剥离最高位（把 $2$ 砍掉）**，得到了 $1345$。

这样切分的好处在于：

1. **$1 \sim 1345$ 变成了递归子问题**：

   求 $1 \sim 1345$ 中 1 的个数，结构和求 $1 \sim 21345$ 一模一样，直接调用自身函数 `NumberOf1(1345)` 即可。

2. **$1346 \sim 21345$ 变成了完美的数学整块**：

   这个区间正好包含 $20000$ 个数字（$21345 - 1345 = 20000$）。这 $20000$ 个数字后 4 位的变化，**恰好完整覆盖了 2 组 $0000 \sim 9999$ 的全组合**！既然是完整组合，就可以直接用排列组合公式一口气算出来，不用逐个累加。

### 三、 详细拆解：$1346 \sim 21345$ 这 20000 个数字怎么算？

在这 $20000$ 个数字中，1 出现的次数被拆成了两部分算：

#### 1. 最高位（万位）上出现 1 的次数

观察万位上的数字：

- 从 $1346$ 到 $21345$，万位出现 1 的数字范围是 $10000 \sim 19999$。

- 这一共有 $10^4 = 10000$ 个数字，所以万位贡献了 **$10000$ 个 1**。

> **注意特例**：如果最高位本身就是 1（比如 $n = 12345$），那么万位出现 1 的范围就是 $10000 \sim 12345$，次数就是去掉最高位后的剩下的数值加 1，即 $2345 + 1 = 2346$ 次。

#### 2. 除最高位外（后 4 位）出现 1 的次数

在 $1346 \sim 21345$ 这 $20000$ 个数字中，看后 4 位（千、百、十、个位）：

- 这 $20000$ 个数字的后 4 位，相当于**经历了 2 组完整的 $0000 \sim 9999$**。

- 对于**单组** $0000 \sim 9999$ 的 4 位数：
  - 任选 1 个位置填入数字 `1`（有 4 种选法）；

  - 剩下 3 个位置每个可以任意填 `0~9`（有 $10^3$ 种填法）；

  - 所以单组中 1 出现的次数是：$4 \times 10^3 = 4000$ 次。

- 因为有 **2 组**（最高位分别是 1 开头和 2 开头），所以后 4 位一共贡献了：$2 \times 4 \times 10^3 = \mathbf{8000}$ **个 1**。

### 四、 最终汇总公式

对于 $n = 21345$：

$$\text{总共 1 的个数} = \underbrace{10000}_{\text{万位上的 1}} + \underbrace{8000}_{\text{后 4 位上的 1}} + \underbrace{\text{NumberOf1}(1345)}_{\text{递归计算剩余部分}}$$

每一次递归都会**砍掉当前数字的最高位**（$21345 \rightarrow 1345 \rightarrow 345 \rightarrow 45 \rightarrow 5$），直到只剩一位数为止。

### 两种解法对比小结

- **之前的数位 DP / 官方题解**：是从**低位到高位（按个位、十位、百位）纵向统计**每一列出现的 1。

- **剑指 Offer 这个解法**：是从**高位到低位（按数字区间）横向切割**，利用组合数学计算整块，剩下的尾巴交给递归。

### Java 代码实现

```Java
class Solution {
    public int countDigitOne(int n) {
        if (n <= 0) return 0;
        return numberOf1(String.valueOf(n));
    }

    private int numberOf1(String s) {
        if (s == null || s.length() == 0) return 0;

        int first = s.charAt(0) - '0'; // 当前最高位的数字
        int length = s.length();       // 当前数字的位数

        // 递归终止条件：只剩 1 位数时
        if (length == 1 && first == 0) return 0;
        if (length == 1 && first > 0) return 1;

        // 1. 计算最高位（如万位）出现 1 的次数
        int numFirstDigit = 0;
        if (first > 1) {
            // 最高位大于 1，比如 21345，万位出现 1 的范围是 10000 ~ 19999，共 10^4 次
            numFirstDigit = powerBase10(length - 1);
        } else if (first == 1) {
            // 最高位等于 1，比如 12345，万位出现 1 的范围是 10000 ~ 12345，共 2345 + 1 次
            numFirstDigit = Integer.parseInt(s.substring(1)) + 1;
        }

        // 2. 计算除去最高位之外，其余位数（如后 4 位）出现 1 的次数
        // 公式：first * (length - 1) * 10^(length - 2)
        int numOtherDigits = first * (length - 1) * powerBase10(length - 2);

        // 3. 递归求解：剥离最高位后剩余的数（例如 21345 去掉最高位 2 变成 1345）
        int numRecursive = numberOf1(s.substring(1));

        // 将三部分相加，即为最终结果
        return numFirstDigit + numOtherDigits + numRecursive;
    }

    // 计算 10 的 n 次方 (10^n)
    private int powerBase10(int n) {
        int result = 1;
        for (int i = 0; i < n; i++) {
            result *= 10;
        }
        return result;
    }
}
```

#### 代码详细讲解

假设输入 n=21345（长度 `length = 5`，最高位 `first = 2`），整个代码的执行逻辑如下：

##### 1. 递归终止条件（ Base Case ）

```Java
if (length == 1 && first == 0) return 0;
if (length == 1 && first > 0) return 1;
```

- 当递归深挖到最后只剩 **1 位数** 时（比如只剩 `5`）：
  - 如果是 0，包含 0 个 1。

  - 如果是 1∼9，在 1∼该数字 的范围内，数字 1 一定且仅出现 **1 次**（即数字 1 本身）。

##### 2. 最高位上 1 的次数（`numFirstDigit`）

```Java
if (first > 1) {
    numFirstDigit = powerBase10(length - 1); // 10^(length - 1)
} else if (first == 1) {
    numFirstDigit = Integer.parseInt(s.substring(1)) + 1;
}
```

- **当 `first > 1`（如 21345）**： 最高位是 2，那么最高位（万位）出现 1 的范围是 10000∼19999，刚好是 $10^4=10000$ 次（即 `powerBase10(5 - 1)`）。

- **当 `first == 1`（如 1345）**： 如果最高位是 1，万位出现 1 的范围是 1000∼1345。万位上有多少个 1，完全取决于去掉最高位后剩下的数字（`345`），也就是 345+1=346 次。`s.substring(1)` 就是把最高位切掉取后面的字符串 `"345"`。

##### 3. 其余位置上 1 的次数（`numOtherDigits`）

```Java
int numOtherDigits = first * (length - 1) * powerBase10(length - 2);
```

对于 21345，后 4 位（千、百、十、个位）出现的 1：

- `length - 1`：后 4 位中任选 1 位填 1，有 4 种选法。

- `powerBase10(length - 2)`：其余 3 位每位有 0∼9 共 10 种可能，即 103。

- `first`：因为最高位是 2，所以后 4 位经历了 2 组完整的 0000∼9999，因此要乘以 2。

- 计算结果：2×4×103=8000。

##### 4. 重点：递归部分（`numRecursive`）

```Java
int numRecursive = numberOf1(s.substring(1));
```

这一步是整个解法的灵魂：**“把大问题拆成已计算的整块 + 未计算的余下部分”**。

- **为什么调用 `s.substring(1)`？** `s.substring(1)` 的作用是**把字符串的第一个字符切掉**。 传入 `"21345"` 时，`s.substring(1)` 得到的是 `"1345"`。

- **为什么要把 `"1345"` 扔进递归？** 因为在前两步中，我们计算的是 1346∼21345 这 20000 个数字中出现的 1（包括万位和后四位）。 但我们要算的是 1∼21345！ 还**漏掉了 1∼1345** 这一段没有算。而计算 1∼1345 中 1 的个数，函数的输入正好就是字符串 `"1345"`！直接调用 `numberOf1("1345")` 即可。

#### 全程递归调用轨迹（Trace）

以 n=21345 为例，程序递归的完整展开过程如下：

1. **第一层：`numberOf1("21345")`**
   - `first = 2`, `length = 5`

   - `numFirstDigit` =104=10000

   - `numOtherDigits` =2×4×103=8000

   - `numRecursive` =numberOf1("1345")

2. **第二层：`numberOf1("1345")`**
   - `first = 1`, `length = 4`

   - `numFirstDigit` =345+1=346 （最高位是 1）

   - `numOtherDigits` =1×3×102=300

   - `numRecursive` =numberOf1("345")

3. **第三层：`numberOf1("345")`**
   - `first = 3`, `length = 3`

   - `numFirstDigit` =102=100

   - `numOtherDigits` =3×2×101=60

   - `numRecursive` =numberOf1("45")

4. **第四层：`numberOf1("45")`**
   - `first = 4`, `length = 2`

   - `numFirstDigit` =101=10

   - `numOtherDigits` =4×1×100=4

   - `numRecursive` =numberOf1("5")

5. **第五层：`numberOf1("5")`**
   - `first = 5`, `length = 1`

   - 触发递归边界 `length == 1 && first > 0`，直接返回 **1**。

**逐层回溯相加**：

- 第三层返回：100+60+(45部分结果)=…

- 最终第一层汇总：(10000+8000)+(346+300)+(100+60)+(10+4)+1=18821。

### 疑问

> 有两组是什么意思？他只是有20000个数字，不是刚好是0-20000，不是从 $1346$ 到 $21345$ 吗？

这也是为什么这个切分方法看似“凭空而来”，但数学上极其巧妙的原因。

虽然区间是 $1346 \sim 21345$，看起来既不从 $0$ 开始，也不在 $19999$ 结束，但如果我们只看它们的**后 4 位**，把这 $20000$ 个数字按万位拆成 3 段，就能看到神奇的“拼接效果”：

#### 第一步：把 $1346 \sim 21345$ 按万位拆成 3 段

1. **第一段**：$1346 \sim 9999$（万位为 0）

2. **第二段**：$10000 \sim 19999$（万位为 1）

3. **第三段**：$20000 \sim 21345$（万位为 2）

#### 第二步：观察每一段的“后 4 位”

我们**只看后 4 位**的变化规律：

- **第二段（$10000 \sim 19999$）**：
  - 后 4 位从 `0000` 一直变到 `9999`。

  - **这独立构成了第 1 组完整的 $0000 \sim 9999$**（共 10000 个）。

- **第一段（$1346 \sim 9999$）**：
  - 后 4 位是 `1346` $\sim$ `9999`（共 8654 个）。

- **第三段（$20000 \sim 21345$）**：
  - 后 4 位是 `0000` $\sim$ `1345`（共 1346 个）。

#### 第三步：神奇的拼接（首尾相凑）

注意看 **第一段** 和 **第三段** 的后 4 位：

- 第三段的后 4 位：`0000` $\sim$ `1345`

- 第一段的后 4 位：`1346` $\sim$ `9999`

把这两段的后 4 位**首尾无缝拼在一起**：

`0000` $\sim$ `1345` + `1346` $\sim$ `9999` = **`0000` $\sim$ `9999`**！

**这又拼出了第 2 组完美的 $0000 \sim 9999$**（共 $1346 + 8654 = 10000$ 个）。

> 这个例子中虽然第一次截出了20000个数字，不过完整的只有10000个，身下的需要拼接的10000个只能计算后四位的情况（当然要算2次）。

#### 总结

为什么书里要切出 $1 \sim 1345$ 去做递归？

就是因为把 $1 \sim 1345$ 扣掉之后，$21345$ 剩下的多余尾巴（$20000 \sim 21345$ 的后 4 位 `0000` $\sim$ `1345`），**刚好能够填补** 开头缺失的（`0000` $\sim$ `1345`），从而让剩下的 $20000$ 个数字的后 4 位，在数学排列上**严丝合缝地湊成了 2 组完整的 $0000 \sim 9999$**。

## 通用动态规划（数位 DP 递归模板）

> TODO: 这个方法目前超出了我的理解范围，暂时不考虑这个思路了。

数位 DP（数位动态规划）的通用核心思想是：**把数字转为字符串，从左到右逐位递归填入数字，并结合记忆化搜索避免重复计算**。

这种“记忆化搜索”模板几乎可以搞定 90% 以上的数位 DP 问题（如统计特定数字出现次数、不含连续 1 的数字个数、数位和为特定值的个数等）。

### 通用数位 DP 递归模板（Java）

以下是以 **LeetCode 233. 数字 1 的个数** 为例的标准通用模板：

```Java
import java.util.Arrays;

class Solution {
    private char[] s;
    private int[][] memo;

    public int countDigitOne(int n) {
        s = Integer.toString(n).toCharArray();
        int m = s.length;
        // memo[i][cnt] 记录：在第 i 位、前面已统计 cnt 个 1 时的通用搜索结果
        memo = new int[m][m];
        for (int[] row : memo) {
            Arrays.fill(row, -1);
        }
        // 初始入口：从第 0 位开始，已统计 0 个 1，初始受到上限限制 (isLimit=true)，前面还未填数字 (isNum=false)
        return dfs(0, 0, true, false);
    }

    /**
     * @param i       当前填第 i 位（从左到右，0-indexed）
     * @param cnt     前面已经填了多少个数字 1（根据题目需求可替换为其他 state，如 mask、sum 等）
     * @param isLimit 当前位填数是否受限制。若为 true，则第 i 位最大只能填 s[i] - '0'；若为 false，可填 0~9
     * @param isNum   前面是否已经填了有效数字。若为 false，表示前面全是前导零，当前位可以继续跳过（不填）
     */
    private int dfs(int i, int cnt, boolean isLimit, boolean isNum) {
        // 递归边界：填完了所有位数，返回当前数字符合条件的统计值
        if (i == s.length) {
            return isNum ? cnt : 0;
        }

        // 记忆化搜索：只有在不受限制 (!isLimit) 且已经填了有效数字 (isNum) 时，答案才是通用的，可以查表
        if (!isLimit && isNum && memo[i][cnt] != -1) {
            return memo[i][cnt];
        }

        int res = 0;

        // 1. 前面还没填有效数字时，当前位可以继续跳过（保持前导零）
        if (!isNum) {
            res += dfs(i + 1, cnt, false, false);
        }

        // 2. 枚举当前位可以填入的有效数字
        int up = isLimit ? s[i] - '0' : 9; // 确定当前位填数的上限
        int low = isNum ? 0 : 1;            // 若前面没填过数字，当前位至少从 1 开始填

        for (int d = low; d <= up; d++) {
            res += dfs(
                i + 1,
                cnt + (d == 1 ? 1 : 0),  // 状态转移：如果是 1，统计加 1
                isLimit && (d == up),     // 下一位是否受限：只有当前受限且填了上限时，下一位才受限
                true                     // 填了数字后，isNum 变为 true
            );
        }

        // 只有在不受限制且是有效数字时，才将结果写入备忘录
        if (!isLimit && isNum) {
            memo[i][cnt] = res;
        }

        return res;
    }
}
```

### 四大关键参数解释

| **参数**          | **含义与作用**          | **为什么需要它？**                                                                                                |
| ----------------- | ----------------------- | ----------------------------------------------------------------------------------------------------------------- |
| **`i`**           | 当前搜到的数位索引      | 确定递归深度，当 `i == len` 时到达叶子节点。                                                                      |
| **`cnt / state`** | 业务状态记录            | 记录前面已经产生的约束或统计值（如：1 的个数、数位和、数字掩码 `mask` 等）。                                      |
| **`isLimit`**     | **上限约束标志**        | 比如 $n = 234$，百位填了 2 时，十位最高只能填 3；若百位填了 1，十位可以自由填 0~9。                               |
| **`isNum`**       | **前导零/有效数字标志** | 用于区分数字中的 `0` 和高位补齐的“前导零”。<br><br> <br><br>_(若题目不统计数字 0 或不关注数字长度，该参数可省略)_ |

### 记忆化搜索的核心逻辑

**为什么只在 `!isLimit && isNum` 时才读取/写入 `memo`？**

1. **`isLimit == true` 时不能记忆**：受限状态下的搜索树是“残缺”的。例如 $n = 234$，百位填 2 时，十位只能搜索 `0, 1, 2, 3`，其结果不能代表十位能填 `0~9` 时的完整组合数。

2. **`isNum == false` 时无需记忆**：处理前导零的路径在整棵递归树中只会被跑一次（最左侧分支），不会重复访问，因此不需要缓存。

### 模板的扩展与套用

解决不同的数位 DP 问题，通常只需修改 **`state` 含义** 和 **`dfs` 中的累加条件**：

- **统计数字 2 的个数**：将 `d == 1` 改为 `d == 2`。

- **统计不含连续 1 的数字个数**（如 LeetCode 600）：将参数 `cnt` 改为 `preIsOne`（上个位置是否填了 1），若 `preIsOne && d == 1` 则剪枝跳过。

- **统计无重复数字的个数**（如 LeetCode 357）：将参数 `cnt` 改为位图 `mask`，记录哪些数字已经填过，填数时要求 `((mask >> d) & 1) == 0`。
